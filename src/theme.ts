import {createTheme} from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#50b848",
            contrastText: "#ffffff"
        },
        secondary: {
            main: "#ffffff"
        },
    },
    typography: {
        fontFamily: "Montserrat",
    }
})